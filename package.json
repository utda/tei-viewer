{
  "name": "tei-viewer",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "release": "npm version patch && git push origin --tags && npm run version && gh release create \"v$VERSION\" --title \"Version $VERSION\"",
    "version": "VERSION=$(cat package.json | grep '\"version\"' | head -1 | awk -F '\"' '{print $4}')"
  },
  "dependencies": {
    "CETEIcean": "^1.9.2",
    "next": "14.1.0",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "14.1.0",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}